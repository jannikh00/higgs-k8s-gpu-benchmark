apiVersion: batch/v1
kind: Job
metadata:
  name: jhof-fetch-data
spec:
  backoffLimit: 4
  template:
    spec:
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/arch: amd64
      containers:
        - name: fetch-data
          image: ghcr.io/jannikh00/higgs-k8s-gpu-benchmark:d083c760463e7d4efa338e9b052ad48ff9ea9c5e
          command: ["python3", "src/fetch_data.py", "--out-dir", "/data"]
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "1"
              memory: 2Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: jhof-higgs-data
