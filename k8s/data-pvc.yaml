apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jhof-higgs-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: rook-ceph-block-fullerton
