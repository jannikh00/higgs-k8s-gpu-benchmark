apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: <netid>-higgs-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: <storage-class>
